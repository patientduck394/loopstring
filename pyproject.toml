[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "loopstring"
version = "0.3.5"
description = "Terminal utilities"
readme = "README.md"
requires-python = ">=3.7"
